const Employee = require('./employee.js')

class Engineer extends Employee {
    constructor(github) {
        this.github = github
      super(name, id, email)
    }
  }

github //github username

getGithub()